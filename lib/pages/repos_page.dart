import 'package:flutter/material.dart';

class ReposPage extends StatelessWidget {
  const ReposPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("repose_page"),
    );
  }
}
